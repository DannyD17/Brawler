using UnityEngine;
using System.Collections;
using UnityEngine.SceneManagement;

public class SceneManage : MonoBehaviour {

    //public static SceneManage instance = null;
    
  
    //void Awake()
    //{

    //    if (instance == null)                                     //Check if instance already exists
    //    {
    //        instance = this;                                      //if not, set instance to this
    //    }
       
    //    else if (instance != this)                             //If instance already exists and it's not this:
    //    {
    //       // Destroy(gameObject);                      //Then destroy this. This enforces our singleton pattern, meaning there can only ever be one instance of a GameManager.
    //    }
       
    //    DontDestroyOnLoad(gameObject);                 //Sets this to not be destroyed when reloading scene

    //    StartLevel1Load();                               // start asyc load of the game map

    //}


    //private void StartLevel1Load()
    //{
    //    SceneManager.LoadSceneAsync(2);  // load scene 3.  this scene should be our main map screen and will be loaded in background while player is in the start menu.

    //}
    
    //public void LoadPregameMenu()
    //{
    //    SceneManager.LoadScene(2);      // load the second scene. this scene should be the pregame character creator/chooser

    //}
    
	

}
